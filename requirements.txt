# Project requirements for Research Assistant Multi-Agent System
# Pin versions where the project used/required specific releases; leave others flexible.

# Web / API
fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# OpenAI + embeddings
openai>=0.27.0
python-dotenv>=1.0.0

# HTTP / scraping
requests>=2.31.0
arxiv>=1.4.0

# Data models
pydantic==2.12.5
pydantic-settings==2.12.0

# Vector DB / RAG
chromadb==1.3.5
langchain>=0.0.300
langchain-openai>=0.3.0
langchain-community>=0.0.7

# LangGraph / Agent pieces
langgraph>=0.1.0

# MCP server (FastMCP client/server)
mcp>=0.1.0

# PDF loading / text splitting / embeddings
PyMuPDF==1.26.6
lark==1.3.1

# Extras (useful tools; optional)
tqdm>=4.65.0

# NOTE:
# - Some imports refer to wrapper packages (e.g. `langchain_openai`, `langchain_community`).
#   The pip package names used above cover the common distributions that expose these modules.
# - If you see import errors after installing these, try installing the specific provider packages
#   referenced by the code (for example `pip install langchain-openai` or `pip install langchain-community`).
